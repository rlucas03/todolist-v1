<!DOCTYPE html>
<html lang="en" dir="ltr">
	<head>
		<meta charset="utf-8">
		<title>To Do List</title>
    <link href="css/styles.css" rel="stylesheet">		
	</head>
	<body>
		<div class="box" id="heading">
					<h1> <%= kindOfDay %></h1>

		</div>

		<div class="box">
			<%	for (let i=0; i <=newListItems.length-1; i++) { %>
			<div class="item">
				<input type="checkbox">
				<p><%= newListItems[i] %></p>

			</div>
		<% } %>

		<form class="item" action = "/" method = "post">
			<input type="text" name="newItem" placeholder="New Item" autocomplete="off">
			<!-- submit button triggers the post request in the form to the /home route -->
			<button type="submit" name="button">+</button>
		</form>
		</div>


<!-- at newListItems we are going to render the value at index i -->

	</body>
</html>